# DESIGN.md — 「极简自然光照」UI 风格 Spec v2.2

> 个人主页设计系统 · 李瑞祥 · AI Agent 产品研发
> 核心理念：底色柔和净白 · 卡片为至纯白（光照层级自然向上发光） · 翡翠微光流动 · 黑曜石质感行为按键

## 1. 设计原则

1. **自然物理光照规律（卡片更亮，绝不比背景暗）**：
   - 遵循人眼对物理界光影的真实直觉：抬升在空中的实体卡片距离光源更近，表面反射更充分，因此**卡片必须使用至纯白（#FFFFFF，100% 明度）**；
   - 处于下沉基底的画布底色采用**柔和净白（#F6F8F7，~97.5% 明度）**；
   - 纯白卡片在柔白底面上自然发光、向上浮起，轮廓由超轻量微边框（`rgba(20,36,26,0.07)`）与软弥散阴影勾勒，层次清晰通透，彻底告别“灰暗卡片陷在白底里”的下沉感。
2. **顶层无缝流动的绿**：极光微光容器从页面顶端（`y = 0`）完整铺开，由 3 团半透明薄荷与翡翠微光缓慢漂移，自然渗透到半透导航胶囊后方，彻底消除任何边缘空白断层。
3. **黑曜石高质感行为按键**：外链行为按键（作品卡与推荐卡的 GitHub 胶囊）采用深曜石深碳色（`#111827`）搭配纯白文字（对比度 > 15:1），悬停加深至纯黑；页面主操作（复制邮箱等）为纯白微边框幽灵胶囊，复制成功时瞬时反色为黑曜石底，形成清晰的触感反馈。克制高雅，不使用刺眼的彩色按键。
4. **黄金人像比例与全端居左**：头像等比优化至 160×224（平板 140×196，手机端 96×134），全端始终保持在左侧。手机端紧邻右侧呈现姓名与职业，消除多余空白。

## 2. 色彩 Tokens

| Token | 值 | 用途 |
| --- | --- | --- |
| `--bg` | `#F6F8F7` | 页面基底画布（柔和净白） |
| `--bg-subtle` | `#F2F5F3` | 区块间微透呼吸带 |
| `--surface-card` | `#FFFFFF` | 卡片表面（至纯白，高于背景明度） |
| `--neutral-soft` | `#EEF2F0` | 默认标签底色、品牌字底 |
| `--ink` | `#0F1713` | 主文字（深邃墨林黑） |
| `--ink-2` | `#46534C` | 次级文字 |
| `--ink-3` | `#829088` | 辅助文字、时间戳、荣誉副标 |
| `--line` | `rgba(20, 36, 26, 0.07)` | 精工微边框 |
| `--line-strong` | `rgba(20, 36, 26, 0.12)` | 强调微边框（头像框、幽灵按键描边） |
| `--brand` | `#059669` | 鲜明翡翠绿（脉冲点、时间线节点、装饰高光） |
| `--brand-deep` | `#047857` | 深翡翠（状态标签文字、图标前景） |
| `--brand-soft` | `#E6F4ED` | 浅薄荷净底（状态标签、项目图标底） |
| `--btn-dark` | `#111827` | 黑曜石行为按键底色 |
| `--btn-dark-hover` | `#000000` | 黑曜石按键悬停加深 |

## 3. 组件清单与交互

- **毛玻璃导航胶囊**：吸顶 `sticky`，半透白 + `backdrop-blur`，ScrollSpy 高亮当前区块；≤ 640px 时导航链接收敛隐藏，仅保留品牌标识。
- **Hero 居左网格**：证件照 + 文案 + Bento 统计卡（桌面 4 列 / 平板 2 列），全端头像严格居左。
- **经历时间线**：翡翠渐变流光轨道 + 品牌色圆形节点，卡片式条目承载教育、实习、项目、科研、竞赛与学生工作。
- **作品卡**：图标 + 技术标签 + 描述，底部黑曜石 GitHub 行为胶囊（新窗口打开，`rel="noopener"`）。
- **Agent Skills 扇形轮盘**：超大曲率半径圆弧排布，物理惯性拖拽（鼠标 / 触摸）+ 空闲缓慢环境漂移；`prefers-reduced-motion` 时停止漂移，仅保留点击跳转。
- **荣誉胶囊网格**：4 / 2 / 1 列响应式胶囊条目，图标 + 名称 + 来源。
- **滚动进度条**：CSS `animation-timeline: scroll()` 原生驱动，旧浏览器由 JS 降级为 rAF 监听。
- **进场动效**：`IntersectionObserver` 渐进增强（`.reveal`），JS 未运行时内容默认可见，杜绝白屏。
- **复制交互**：Clipboard API + `execCommand` 降级，Toast 提示 + 按钮黑曜石反色触感反馈。

## 4. 无障碍

- 全站遵循 `prefers-reduced-motion`：关闭极光漂移、时间线流光、脉冲与进场动效；
- 提供跳转链接（skip-link）、`:focus-visible` 焦点环，装饰性图标统一 `aria-hidden`；
- 主文案使用 `--ink` / `--ink-2` 保证可读性，`--ink-3` 仅用于时间戳、副标等弱化信息。
