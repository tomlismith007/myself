# 个人网站统计系统完整方案：Cloudflare Worker + D1

> 基于 Cloudflare 全栈 Serverless 架构的个人网站访问统计系统，从架构分析到完整可部署代码。

---

## 目录

1. [方案概述与架构](#1-方案概述与架构)
2. [可行性深度分析](#2-可行性深度分析)
3. [关键技术坑与解决方案](#3-关键技术坑与解决方案)
4. [完整项目结构](#4-完整项目结构)
5. [数据库 Schema（修正版）](#5-数据库-schema修正版)
6. [前端统计脚本（完整可运行）](#6-前端统计脚本完整可运行)
7. [Worker 完整代码](#7-worker-完整代码)
8. [管理后台（ECharts）](#8-管理后台echarts)
9. [部署步骤（完整命令）](#9-部署步骤完整命令)
10. [安全与隐私合规](#10-安全与隐私合规)
11. [性能优化](#11-性能优化)
12. [成本估算](#12-成本估算)
13. [演进路线 V1 → V2 → V3](#13-演进路线-v1--v2--v3)
14. [服务器资源规划](#14-服务器资源规划)
15. [常见问题 FAQ](#15-常见问题-faq)

---

## 1. 方案概述与架构

### 1.1 整体架构图

```
用户浏览器
   │
   ▼
Cloudflare CDN (全球边缘节点)
   │
   ├──────────────────────┐
   │                      │
   ▼                      ▼
个人主页                统计 Worker
Cloudflare Pages        stats.example.com
   │                      │
   │ 页面加载时触发        │
   │ JS 采集脚本 ────────►│ POST /collect
   │                      │
   │                      ▼
   │              Cloudflare D1
   │              (Serverless SQL)
   │                      │
   │              ┌───────┴───────┐
   │              │               │
   │              ▼               ▼
   │         page_views      daily_stats
   │         (原始明细)      (每日聚合)
   │              │               │
   │              └───────┬───────┘
   │                      │
   ▼                      ▼
页面展示统计组件    管理后台 /admin
(总PV/UV/在线)     (ECharts 可视化)
```

### 1.2 核心组件职责

| 组件 | 技术 | 职责 |
|------|------|------|
| 个人主页 | Cloudflare Pages | 展示网站内容，嵌入统计采集脚本 |
| 统计 Worker | Cloudflare Workers | 接收采集数据、写入 D1、提供统计查询 API、托管管理后台 |
| 数据库 | Cloudflare D1 | 存储访问明细、每日聚合、在线状态 |
| CDN/DNS/证书 | Cloudflare 原生 | 全球加速、DNS 解析、自动 HTTPS 证书 |

### 1.3 为什么不用新加坡服务器

对于个人主页（日 PV 几十到几百），自建服务器统计链路存在以下问题：

- **维护成本高**：Nginx 配置、HTTPS 证书续期、API 服务、MySQL 运维、防火墙规则、数据库备份
- **跨境网络延迟**：国内用户访问新加坡服务器存在 50-150ms 额外延迟
- **单点故障**：服务器宕机则统计完全丢失
- **资源浪费**：2C1G 服务器跑一个低流量统计服务，CPU/内存利用率极低

而 Cloudflare Worker + D1 方案：
- 全球 300+ 边缘节点，用户就近访问
- 按用量计费，个人站基本在免费额度内
- 零运维，自动扩缩容
- 与你的网站同处 Cloudflare 生态，配置极简

---

## 2. 可行性深度分析

### 2.1 技术可行性

| 维度 | 评估 | 说明 |
|------|------|------|
| Worker 接收请求 | ✅ 完全可行 | Workers 支持 HTTP 请求处理，免费版每天 10 万请求 |
| D1 数据存储 | ✅ 完全可行 | D1 基于 SQLite，支持标准 SQL，免费版 5GB 存储 |
| Worker ↔ D1 绑定 | ✅ 完全可行 | 原生集成，通过 `env.DB` 直接调用 |
| 自定义域名 | ✅ 完全可行 | Worker 支持绑定自定义子域名，自动管理 DNS 和证书 |
| 定时任务 | ✅ 完全可行 | Worker 支持 Cron Trigger（`scheduled()` 处理器） |
| 静态资源托管 | ✅ 完全可行 | Worker 可直接返回 HTML/CSS/JS，管理后台可内嵌 |
| 地理信息获取 | ✅ 完全可行 | `request.cf` 对象包含国家、城市、ASN 等信息 |

### 2.2 成本可行性

**免费额度内可长期运行**，详见 [第 12 节成本估算](#12-成本估算)。

### 2.3 潜在风险

| 风险 | 等级 | 缓解措施 |
|------|------|----------|
| 第三方 Cookie 被浏览器拦截 | 🔴 高 | 改用第一方 Cookie / localStorage 方案（见 3.1） |
| D1 免费额度超限 | 🟡 中 | 个人站量级远低于限额；聚合查询避免全表扫描 |
| 管理后台 Token 泄露 | 🟡 中 | 使用 Secret 存储；后续升级 Cloudflare Access |
| 数据丢失 | 🟢 低 | D1 内置 Time Travel（30 天时点恢复） |
| Cloudflare 服务中断 | 🟢 低 | 采集失败不影响主站；可降级为本地暂存 |

---

## 3. 关键技术坑与解决方案

### 3.1 🔴 最重要：第三方 Cookie 被浏览器拦截

**问题描述**：

原方案中，统计脚本运行在 `www.example.com`，Worker 在 `stats.example.com`。Worker 通过 `Set-Cookie` 给浏览器设置 `visitor_id`。

这在现代浏览器中会**严重失效**：

- **Safari (ITP)**：默认阻止第三方 Cookie，且会在 7 天后清除第一方脚本写入的存储
- **Firefox (ETP)**：严格模式下阻止第三方 Cookie
- **Chrome**：已逐步限制第三方 Cookie（2024-2025 分阶段淘汰）

结果：每次访问都生成新的 `visitor_id`，UV 统计完全失真（UV ≈ PV）。

**解决方案（三选一，推荐 A）**：

#### 方案 A：第一方生成 visitor_id（推荐）

在主站页面的 JavaScript 中生成 `visitor_id`，存储在 `localStorage`（第一方），然后在采集请求的 **body** 中传给 Worker。Worker 不再依赖 Cookie。

```javascript
// 前端统计脚本中
function getVisitorId() {
  let vid = localStorage.getItem('pa_visitor_id');
  if (!vid) {
    vid = crypto.randomUUID();
    localStorage.setItem('pa_visitor_id', vid);
  }
  return vid;
}
```

**优点**：完全规避第三方 Cookie 问题，所有浏览器兼容。
**缺点**：用户清除浏览器数据后 ID 会重置（可接受）；跨子域不共享（如果只有一个主站则无影响）。

#### 方案 B：统计端点挂在主站路径下

通过 Cloudflare Worker Routes，把 `www.example.com/__collect/*` 路由到统计 Worker。这样采集请求是第一方请求，Cookie 也是第一方。

在 `wrangler.toml` 中配置：
```toml
routes = [
  { pattern = "www.example.com/__collect/*", script_name = "personal-analytics" }
]
```

**优点**：第一方 Cookie，可跨页面共享。
**缺点**：需要主站域名在 Cloudflare 代理下（已满足）；路由配置稍复杂。

#### 方案 C：Cookie 设置 Domain 为根域

Worker 设置 `Set-Cookie: visitor_id=xxx; Domain=.example.com; ...`。

**缺点**：仍然可能被浏览器视为第三方 Cookie（取决于具体策略）；不推荐作为唯一方案。

> **本文档后续代码采用方案 A（第一方生成 visitor_id）**，这是最稳妥、兼容性最好的方案。

### 3.2 CORS 跨域配置

由于采集请求是从 `www.example.com` 发往 `stats.example.com`，属于跨域请求。必须正确配置 CORS：

```javascript
function corsResponse(origin) {
  return new Response(null, {
    headers: {
      "Access-Control-Allow-Origin": origin || "https://www.example.com",
      "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
      "Access-Control-Allow-Headers": "Content-Type, Authorization",
      "Access-Control-Max-Age": "86400",
    },
  });
}
```

**注意**：
- `Access-Control-Allow-Origin` **不能用 `*`**，必须返回具体的请求来源（从 `Origin` header 读取并校验白名单）
- 如果后续改用 Cookie 方案，还需加 `Access-Control-Allow-Credentials: true`

### 3.3 D1 时区问题

D1 的 `CURRENT_TIMESTAMP` 返回的是 **UTC 时间**。如果你在东八区（北京时间），"今日访问"的统计会出现 8 小时偏差。

**解决方案**：在 SQL 查询中统一用 UTC 日期，在前端展示时转换为用户本地时区。或者在写入时存 UTC，查询时用 `date(created_at, '+8 hours')` 转换。

```sql
-- 按北京时间统计今日 PV
SELECT COUNT(*) AS pv FROM page_views
WHERE date(created_at, '+8 hours') = date('now', '+8 hours');
```

> 建议在配置中设置一个 `TIMEZONE_OFFSET` 变量，统一管理时区偏移。

### 3.4 daily_stats 表的更新逻辑

原方案设计了 `daily_stats` 表但 `collect` 接口中没有更新它。有两种策略：

#### 策略 1：实时 Upsert（推荐用于低流量）

每次采集时，同时更新 `daily_stats`：

```sql
INSERT INTO daily_stats (stat_date, page_views, unique_visitors)
VALUES (date('now'), 1, 0)
ON CONFLICT(stat_date) DO UPDATE SET page_views = page_views + 1;
```

UV 不能简单 +1，需要判断是否新访客。可以在 `online_visitors` 或单独的 `daily_visitors` 表中记录当日访客集合。

#### 策略 2：定时聚合（推荐用于中高流量）

通过 Cron Trigger 每小时/每天从 `page_views` 聚合到 `daily_stats`。这样减少每次请求的写入次数。

> 个人站日 PV < 1000，**策略 1 完全够用**，且实现简单。

### 3.5 online_visitors 表的清理

`online_visitors` 表如果不清理会无限增长。需要通过 Cron Trigger 定期清理：

```javascript
// scheduled() 处理器中
async function cleanupOnline(env) {
  await env.DB.prepare(
    "DELETE FROM online_visitors WHERE last_seen < datetime('now', '-5 minutes')"
  ).run();
}
```

在 `wrangler.toml` 中配置：
```toml
[triggers]
crons = ["*/5 * * * *"]  # 每 5 分钟执行一次
```

### 3.6 SPA 单页应用的路由统计

如果你的个人主页是 React/Vue 等 SPA，页面切换不会触发完整的页面加载，统计脚本只会在首次加载时执行一次。

**解决方案**：监听路由变化（History API / 框架路由事件），手动触发采集：

```javascript
// 监听 History 变化
let lastUrl = location.href;
const observer = new MutationObserver(() => {
  if (location.href !== lastUrl) {
    lastUrl = location.href;
    trackPageView();  // 重新采集
  }
});
observer.observe(document, { subtree: true, childList: true });
```

### 3.7 采集失败不影响主站

统计请求失败绝不能影响用户浏览页面。使用 `navigator.sendBeacon()` 或 `fetch` with `keepalive`，并包裹 try-catch：

```javascript
try {
  if (navigator.sendBeacon) {
    const blob = new Blob([JSON.stringify(payload)], { type: 'application/json' });
    navigator.sendBeacon('https://stats.example.com/collect', blob);
  } else {
    fetch('https://stats.example.com/collect', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload),
      keepalive: true,
    });
  }
} catch (e) {
  // 静默失败，不影响页面
}
```

> `sendBeacon` 是异步的、即使页面关闭也会尝试发送，非常适合统计场景。但它不支持自定义 Header，所以如果需要 Authorization，只能用 fetch + keepalive。

### 3.8 D1 rows read 额度消耗

D1 免费版每天 500 万 **rows read**。注意：rows read 是查询**扫描的行数**，不是返回的行数。

`SELECT COUNT(*) FROM page_views` 会扫描全表所有行。如果表中有 10 万行，一次查询就消耗 10 万 rows read。

**优化**：
- 总 PV/UV 从 `daily_stats` 聚合表查询（只扫描 30 行）
- 趋势查询限制时间范围（`WHERE created_at >= datetime('now', '-30 days')`）
- 对 `created_at`、`page`、`country` 等常用查询字段建索引

---

## 4. 完整项目结构

```
personal-analytics/
│
├── src/
│   ├── index.js          # Worker 入口，路由分发
│   ├── collect.js        # 采集接口逻辑
│   ├── stats.js          # 统计查询接口
│   ├── auth.js           # 管理后台鉴权
│   ├── cleanup.js        # 定时清理与聚合
│   └── cors.js           # CORS 工具函数
│
├── admin/
│   ├── index.html        # 管理后台页面
│   ├── app.js            # 后台逻辑 + ECharts
│   └── style.css         # 后台样式
│
├── public/
│   └── tracker.js        # 前端采集脚本（可直接嵌入网站）
│
├── schema.sql            # 数据库建表脚本
├── wrangler.toml         # Cloudflare 配置
├── package.json          # 项目依赖
└── README.md             # 项目说明
```

---

## 5. 数据库 Schema（修正版）

```sql
-- schema.sql

-- 访问明细表：记录每一次页面访问
CREATE TABLE IF NOT EXISTS page_views (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  visitor_id    TEXT    NOT NULL,           -- 前端生成的访客唯一ID
  page          TEXT    NOT NULL,           -- 访问的页面路径
  referrer      TEXT    DEFAULT '',         -- 来源页面
  country       TEXT    DEFAULT 'Unknown',  -- 国家代码（来自 Cloudflare）
  city          TEXT    DEFAULT '',          -- 城市（来自 Cloudflare）
  user_agent    TEXT    DEFAULT '',          -- 用户代理字符串
  device_type   TEXT    DEFAULT 'unknown',   -- desktop/mobile/tablet/bot
  screen_width  INTEGER DEFAULT 0,
  screen_height INTEGER DEFAULT 0,
  lang          TEXT    DEFAULT '',          -- 浏览器语言
  created_at    TEXT    DEFAULT CURRENT_TIMESTAMP  -- UTC 时间
);

-- 索引：加速按时间、页面、国家、访客的查询
CREATE INDEX IF NOT EXISTS idx_pv_created_at ON page_views(created_at);
CREATE INDEX IF NOT EXISTS idx_pv_page       ON page_views(page);
CREATE INDEX IF NOT EXISTS idx_pv_country    ON page_views(country);
CREATE INDEX IF NOT EXISTS idx_pv_visitor    ON page_views(visitor_id);

-- 每日聚合表：预计算每日 PV/UV，避免全表扫描
CREATE TABLE IF NOT EXISTS daily_stats (
  stat_date        TEXT PRIMARY KEY,          -- 日期 (YYYY-MM-DD, UTC)
  page_views       INTEGER DEFAULT 0,
  unique_visitors  INTEGER DEFAULT 0
);

-- 每日访客表：记录当日有哪些访客，用于精确计算 UV
CREATE TABLE IF NOT EXISTS daily_visitors (
  stat_date   TEXT NOT NULL,
  visitor_id  TEXT NOT NULL,
  first_seen  TEXT DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (stat_date, visitor_id)
);

-- 在线访客表：最近 5 分钟内活跃的访客
CREATE TABLE IF NOT EXISTS online_visitors (
  visitor_id  TEXT PRIMARY KEY,
  last_seen   TEXT NOT NULL,
  last_page   TEXT DEFAULT '/'
);

-- 事件表（V3 预留）：自定义事件（按钮点击、下载等）
CREATE TABLE IF NOT EXISTS events (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  visitor_id  TEXT    NOT NULL,
  event_name  TEXT    NOT NULL,           -- 如 github_click, resume_download
  event_value TEXT    DEFAULT '',
  page        TEXT    DEFAULT '',
  created_at  TEXT    DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_events_name ON events(event_name);
CREATE INDEX IF NOT EXISTS idx_events_visitor ON events(visitor_id);
```

**设计说明**：

1. **新增 `daily_visitors` 表**：原方案只用 `daily_stats` 存 UV 总数，但 UV 无法简单 +1（需要判断是否新访客）。`daily_visitors` 用 `(stat_date, visitor_id)` 联合主键，插入时 `INSERT OR IGNORE`，天然去重。然后 `daily_stats` 的 UV 可以从这个表 COUNT。

2. **新增 `city`、`device_type`、`lang` 字段**：Cloudflare 的 `request.cf` 提供城市信息；设备类型从 User-Agent 解析；浏览器语言从 `Accept-Language` 获取。这些都是有价值的分析维度。

3. **新增 `events` 表**：为 V3 事件分析预留，V1 可不使用。

4. **索引**：对常用查询字段建索引，大幅减少 rows read 消耗。

---

## 6. 前端统计脚本（完整可运行）

将以下脚本保存为 `public/tracker.js`，然后在个人主页的 `</body>` 前引入：

```html
<script defer src="https://stats.example.com/tracker.js"></script>
```

或者直接内联到页面中（减少一次请求）。

```javascript
// public/tracker.js
(function () {
  'use strict';

  // ====== 配置 ======
  const CONFIG = {
    endpoint: 'https://stats.example.com/collect',  // 采集端点
    siteId: 'default',                                // 站点标识（多站点时区分）
    trackHashChange: true,                            // 是否追踪 SPA hash 路由变化
    doNotTrack: true,                                 // 是否尊重 DNT 设置
  };

  // ====== 工具函数 ======

  // 获取或生成 visitor_id（存在 localStorage，第一方存储，无第三方 Cookie 问题）
  function getVisitorId() {
    try {
      let vid = localStorage.getItem('pa_visitor_id');
      if (!vid) {
        vid = (crypto && crypto.randomUUID)
          ? crypto.randomUUID()
          : 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function (c) {
              const r = Math.random() * 16 | 0;
              return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16);
            });
        localStorage.setItem('pa_visitor_id', vid);
      }
      return vid;
    } catch (e) {
      // localStorage 不可用时，每次生成临时 ID（降级）
      return 'temp-' + Date.now() + '-' + Math.random().toString(36).substr(2, 9);
    }
  }

  // 解析设备类型
  function getDeviceType() {
    const ua = navigator.userAgent;
    if (/bot|crawl|spider|slurp|bingpreview/i.test(ua)) return 'bot';
    if (/iPad|Tablet|PlayBook|Silk/i.test(ua)) return 'tablet';
    if (/Mobi|Android|iPhone|iPod|BlackBerry|IEMobile|Opera Mini/i.test(ua)) return 'mobile';
    return 'desktop';
  }

  // 构建采集数据
  function buildPayload() {
    return {
      visitor_id: getVisitorId(),
      site_id: CONFIG.siteId,
      page: location.pathname + location.search,
      referrer: document.referrer || '',
      title: document.title || '',
      screen_width: screen.width || 0,
      screen_height: screen.height || 0,
      viewport_width: window.innerWidth || 0,
      viewport_height: window.innerHeight || 0,
      device_type: getDeviceType(),
      lang: (navigator.language || navigator.userLanguage || '').substring(0, 2),
      timestamp: new Date().toISOString(),
    };
  }

  // 发送采集数据（优先 sendBeacon，降级 fetch）
  function send(payload) {
    const body = JSON.stringify(payload);
    try {
      if (navigator.sendBeacon) {
        const blob = new Blob([body], { type: 'application/json' });
        navigator.sendBeacon(CONFIG.endpoint, blob);
        return;
      }
    } catch (e) { /* 降级到 fetch */ }

    try {
      fetch(CONFIG.endpoint, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: body,
        keepalive: true,
        mode: 'cors',
      }).catch(function () { /* 静默失败 */ });
    } catch (e) { /* 采集失败不影响页面 */ }
  }

  // 检查是否应该追踪
  function shouldTrack() {
    if (CONFIG.doNotTrack) {
      const dnt = navigator.doNotTrack || window.doNotTrack || navigator.msDoNotTrack;
      if (dnt === '1' || dnt === 'yes') return false;
    }
    // 不追踪本地开发环境
    if (location.hostname === 'localhost' || location.hostname === '127.0.0.1') return false;
    return true;
  }

  // 执行一次页面访问采集
  function trackPageView() {
    if (!shouldTrack()) return;
    send(buildPayload());
  }

  // ====== 自定义事件追踪（V3 用，V1 可忽略）======
  window.pa = window.pa || {};
  window.pa.track = function (eventName, eventValue) {
    if (!shouldTrack()) return;
    const payload = buildPayload();
    payload.event_name = eventName;
    payload.event_value = eventValue || '';
    send(payload);
  };

  // ====== 初始化 ======
  function init() {
    // 首次页面加载采集
    trackPageView();

    // SPA 路由变化监听（History API）
    let lastUrl = location.href;
    const origPushState = history.pushState;
    history.pushState = function () {
      origPushState.apply(this, arguments);
      if (location.href !== lastUrl) {
        lastUrl = location.href;
        setTimeout(trackPageView, 100);
      }
    };
    window.addEventListener('popstate', function () {
      if (location.href !== lastUrl) {
        lastUrl = location.href;
        setTimeout(trackPageView, 100);
      }
    });

    // Hash 路由变化
    if (CONFIG.trackHashChange) {
      window.addEventListener('hashchange', function () {
        setTimeout(trackPageView, 100);
      });
    }
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
```

**脚本特性**：
- ✅ 第一方 `localStorage` 存储 visitor_id，无第三方 Cookie 问题
- ✅ 支持 SPA 路由变化自动采集（History API + Hash）
- ✅ `sendBeacon` 优先，页面关闭也能发送
- ✅ 尊重 `Do Not Track` 设置
- ✅ 本地开发环境自动跳过
- ✅ 采集失败完全静默，不影响主站
- ✅ 暴露 `window.pa.track()` 用于自定义事件（V3）

---

## 7. Worker 完整代码

### 7.1 入口与路由 `src/index.js`

```javascript
// src/index.js
import { handleCollect } from './collect.js';
import { handleStats } from './stats.js';
import { checkAuth } from './auth.js';
import { handleCleanup } from './cleanup.js';
import { corsResponse, ALLOWED_ORIGINS } from './cors.js';

export default {
  // HTTP 请求入口
  async fetch(request, env, ctx) {
    const url = new URL(request.url);
    const origin = request.headers.get('Origin') || '';

    // CORS 预检
    if (request.method === 'OPTIONS') {
      return corsResponse(origin);
    }

    // 采集接口（公开，无需鉴权）
    if (url.pathname === '/collect' && request.method === 'POST') {
      return handleCollect(request, env, ctx);
    }

    // 前端统计脚本（公开）
    if (url.pathname === '/tracker.js') {
      return serveTracker(env);
    }

    // 管理后台页面（公开访问，但数据接口需要鉴权）
    if (url.pathname === '/admin' || url.pathname === '/admin/') {
      return serveAdmin();
    }

    // ====== 以下接口需要鉴权 ======
    const authResult = checkAuth(request, env);
    if (!authResult.ok) {
      return new Response(JSON.stringify({ error: authResult.error }), {
        status: 401,
        headers: { 'Content-Type': 'application/json', ...corsHeaders(origin) },
      });
    }

    // 统计查询接口
    if (url.pathname.startsWith('/stats')) {
      return handleStats(request, env, url);
    }

    return new Response('Not Found', {
      status: 404,
      headers: { 'Content-Type': 'text/plain' },
    });
  },

  // 定时任务入口（Cron Trigger）
  async scheduled(event, env, ctx) {
    ctx.waitUntil(handleCleanup(env));
  },
};

// 服务管理后台 HTML
function serveAdmin() {
  // 见第 8 节，这里返回 admin/index.html 内容
  // 实际项目中可以用 import 或者直接内联
  return new Response(ADMIN_HTML, {
    headers: { 'Content-Type': 'text/html; charset=utf-8' },
  });
}

// 服务前端统计脚本
function serveTracker(env) {
  return new Response(TRACKER_JS, {
    headers: {
      'Content-Type': 'application/javascript; charset=utf-8',
      'Cache-Control': 'public, max-age=3600',
    },
  });
}

function corsHeaders(origin) {
  return {
    'Access-Control-Allow-Origin': ALLOWED_ORIGINS.includes(origin) ? origin : '',
    'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',
    'Access-Control-Allow-Headers': 'Content-Type, Authorization',
  };
}
```

### 7.2 采集接口 `src/collect.js`

```javascript
// src/collect.js
import { corsResponse, ALLOWED_ORIGINS } from './cors.js';

export async function handleCollect(request, env, ctx) {
  const origin = request.headers.get('Origin') || '';

  // 校验来源（防止其他网站滥用你的采集端点）
  if (origin && !ALLOWED_ORIGINS.includes(origin)) {
    return new Response(JSON.stringify({ error: 'origin not allowed' }), {
      status: 403,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  let data;
  try {
    data = await request.json();
  } catch (e) {
    return new Response(JSON.stringify({ error: 'invalid JSON' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  // 从 Cloudflare 请求上下文获取地理信息
  const cf = request.cf || {};
  const country = cf.country || 'Unknown';
  const city = cf.city || '';

  const visitorId = data.visitor_id || '';
  if (!visitorId) {
    return new Response(JSON.stringify({ error: 'missing visitor_id' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  // 过滤明显的 Bot（可根据需要调整）
  const userAgent = request.headers.get('User-Agent') || '';
  const isBot = /bot|crawl|spider|slurp|bingpreview|headless/i.test(userAgent);
  const deviceType = isBot ? 'bot' : (data.device_type || 'unknown');

  // 使用 ctx.waitUntil 异步写入，不阻塞响应
  ctx.waitUntil((async () => {
    try {
      const today = new Date().toISOString().split('T')[0]; // UTC 日期

      // 1. 写入访问明细
      await env.DB.prepare(`
        INSERT INTO page_views
          (visitor_id, page, referrer, country, city, user_agent, device_type,
           screen_width, screen_height, lang)
        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
      `).bind(
        visitorId,
        data.page || '/',
        data.referrer || '',
        country,
        city,
        userAgent,
        deviceType,
        data.screen_width || 0,
        data.screen_height || 0,
        data.lang || ''
      ).run();

      // 2. 更新每日聚合 PV
      await env.DB.prepare(`
        INSERT INTO daily_stats (stat_date, page_views, unique_visitors)
        VALUES (?, 1, 0)
        ON CONFLICT(stat_date) DO UPDATE SET page_views = page_views + 1
      `).bind(today).run();

      // 3. 记录当日访客（INSERT OR IGNORE 天然去重）
      await env.DB.prepare(`
        INSERT OR IGNORE INTO daily_visitors (stat_date, visitor_id)
        VALUES (?, ?)
      `).bind(today, visitorId).run();

      // 4. 更新当日 UV（从 daily_visitors 重新计数）
      const uvResult = await env.DB.prepare(`
        SELECT COUNT(*) AS uv FROM daily_visitors WHERE stat_date = ?
      `).bind(today).first('uv');

      await env.DB.prepare(`
        UPDATE daily_stats SET unique_visitors = ? WHERE stat_date = ?
      `).bind(uvResult, today).run();

      // 5. 更新在线访客
      await env.DB.prepare(`
        INSERT INTO online_visitors (visitor_id, last_seen, last_page)
        VALUES (?, datetime('now'), ?)
        ON CONFLICT(visitor_id) DO UPDATE SET last_seen = datetime('now'), last_page = ?
      `).bind(visitorId, data.page || '/', data.page || '/').run();

    } catch (err) {
      console.error('collect write failed:', err);
    }
  })());

  // 立即返回成功，不等数据库写入完成
  return new Response(JSON.stringify({ success: true }), {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Access-Control-Allow-Origin': ALLOWED_ORIGINS.includes(origin) ? origin : '',
    },
  });
}
```

**关键设计**：
- `ctx.waitUntil()` 异步写入数据库，响应立即返回，用户无感知延迟
- 来源校验防止其他网站滥用你的采集端点
- Bot 识别并标记为 `device_type = 'bot'`，后续统计可过滤
- 5 步写入保证数据一致性：明细 → 日 PV → 日访客去重 → 日 UV → 在线状态

### 7.3 统计查询接口 `src/stats.js`

```javascript
// src/stats.js

export async function handleStats(request, env, url) {
  const path = url.pathname;

  try {
    switch (path) {
      case '/stats':
        return json(await getOverview(env));
      case '/stats/today':
        return json(await getToday(env));
      case '/stats/trend':
        return json(await getTrend(env, url));
      case '/stats/pages':
        return json(await getPages(env));
      case '/stats/countries':
        return json(await getCountries(env));
      case '/stats/devices':
        return json(await getDevices(env));
      case '/stats/referrers':
        return json(await getReferrers(env));
      case '/stats/online':
        return json(await getOnline(env));
      case '/stats/recent':
        return json(await getRecentVisitors(env));
      default:
        return json({ error: 'not found' }, 404);
    }
  } catch (err) {
    console.error('stats error:', err);
    return json({ error: 'internal error' }, 500);
  }
}

function json(data, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { 'Content-Type': 'application/json' },
  });
}

// 总览：总 PV、总 UV、今日 PV、今日 UV、在线
async function getOverview(env) {
  // 总 PV/UV 从 daily_stats 聚合（避免全表扫描）
  const total = await env.DB.prepare(`
    SELECT SUM(page_views) AS total_pv, SUM(unique_visitors) AS total_uv
    FROM daily_stats
  `).first();

  const today = await getToday(env);
  const online = await getOnline(env);

  return {
    total_pv: total?.total_pv || 0,
    total_uv: total?.total_uv || 0,
    today_pv: today.today_pv,
    today_uv: today.today_uv,
    online: online.online,
  };
}

// 今日统计
async function getToday(env) {
  const today = new Date().toISOString().split('T')[0];
  const result = await env.DB.prepare(`
    SELECT page_views, unique_visitors FROM daily_stats WHERE stat_date = ?
  `).bind(today).first();

  return {
    today_pv: result?.page_views || 0,
    today_uv: result?.unique_visitors || 0,
  };
}

// 最近 N 天趋势（默认 30 天）
async function getTrend(env, url) {
  const days = parseInt(url.searchParams.get('days') || '30', 10);
  const result = await env.DB.prepare(`
    SELECT stat_date AS day, page_views AS pv, unique_visitors AS uv
    FROM daily_stats
    WHERE stat_date >= date('now', ?)
    ORDER BY stat_date ASC
  `).bind(`-${days} days`).all();

  return result.results || [];
}

// 热门页面 TOP 10
async function getPages(env) {
  const result = await env.DB.prepare(`
    SELECT page, COUNT(*) AS pv, COUNT(DISTINCT visitor_id) AS uv
    FROM page_views
    WHERE device_type != 'bot'
    GROUP BY page
    ORDER BY pv DESC
    LIMIT 10
  `).all();
  return result.results || [];
}

// 国家排行
async function getCountries(env) {
  const result = await env.DB.prepare(`
    SELECT country, COUNT(*) AS pv, COUNT(DISTINCT visitor_id) AS uv
    FROM page_views
    WHERE device_type != 'bot'
    GROUP BY country
    ORDER BY uv DESC
    LIMIT 20
  `).all();
  return result.results || [];
}

// 设备分布
async function getDevices(env) {
  const result = await env.DB.prepare(`
    SELECT device_type, COUNT(*) AS pv, COUNT(DISTINCT visitor_id) AS uv
    FROM page_views
    WHERE device_type != 'bot'
    GROUP BY device_type
    ORDER BY pv DESC
  `).all();
  return result.results || [];
}

// 来源网站
async function getReferrers(env) {
  const result = await env.DB.prepare(`
    SELECT referrer, COUNT(*) AS pv
    FROM page_views
    WHERE referrer != '' AND device_type != 'bot'
    GROUP BY referrer
    ORDER BY pv DESC
    LIMIT 20
  `).all();
  return result.results || [];
}

// 在线人数（最近 5 分钟）
async function getOnline(env) {
  const result = await env.DB.prepare(`
    SELECT COUNT(*) AS online FROM online_visitors
    WHERE last_seen >= datetime('now', '-5 minutes')
  `).first();
  return { online: result?.online || 0 };
}

// 最近访客
async function getRecentVisitors(env) {
  const result = await env.DB.prepare(`
    SELECT visitor_id, page, country, device_type, created_at
    FROM page_views
    WHERE device_type != 'bot'
    ORDER BY created_at DESC
    LIMIT 50
  `).all();
  return result.results || [];
}
```

### 7.4 鉴权 `src/auth.js`

```javascript
// src/auth.js
export function checkAuth(request, env) {
  // 从 Worker Secret 中读取 ADMIN_TOKEN
  const expectedToken = env.ADMIN_TOKEN;
  if (!expectedToken) {
    return { ok: false, error: 'ADMIN_TOKEN not configured' };
  }

  const authHeader = request.headers.get('Authorization') || '';
  const token = authHeader.replace(/^Bearer\s+/i, '');

  if (token !== expectedToken) {
    return { ok: false, error: 'unauthorized' };
  }

  return { ok: true };
}
```

### 7.5 定时清理 `src/cleanup.js`

```javascript
// src/cleanup.js
export async function handleCleanup(env) {
  // 1. 清理超过 5 分钟的在线访客
  await env.DB.prepare(`
    DELETE FROM online_visitors WHERE last_seen < datetime('now', '-5 minutes')
  `).run();

  // 2. 清理超过 90 天的访问明细（可根据需要调整保留期）
  // 注意：删除前可先导出到 R2 做归档（V2 功能）
  await env.DB.prepare(`
    DELETE FROM page_views WHERE created_at < datetime('now', '-90 days')
  `).run();

  // 3. 清理超过 90 天的每日访客记录
  await env.DB.prepare(`
    DELETE FROM daily_visitors WHERE stat_date < date('now', '-90 days')
  `).run();

  console.log('cleanup completed at', new Date().toISOString());
}
```

### 7.6 CORS 工具 `src/cors.js`

```javascript
// src/cors.js
// 允许的来源白名单（你的网站域名）
export const ALLOWED_ORIGINS = [
  'https://www.example.com',
  'https://example.com',
  // 开发环境（可选）
  // 'http://localhost:3000',
];

export function corsResponse(origin) {
  const allowed = ALLOWED_ORIGINS.includes(origin) ? origin : '';
  return new Response(null, {
    status: 204,
    headers: {
      'Access-Control-Allow-Origin': allowed,
      'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',
      'Access-Control-Allow-Headers': 'Content-Type, Authorization',
      'Access-Control-Max-Age': '86400',
    },
  });
}
```

---

## 8. 管理后台（ECharts）

管理后台是一个单页 HTML，内嵌 ECharts（通过 CDN 加载），通过 fetch 调用统计 API。

```html
<!-- admin/index.html -->
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Personal Analytics</title>
  <script src="https://cdn.jsdelivr.net/npm/echarts@5.5.0/dist/echarts.min.js"></script>
  <style>
    /* 样式见 style.css，此处省略，实际项目中独立文件 */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
           background: #0f172a; color: #e2e8f0; padding: 20px; }
    .header { display: flex; justify-content: space-between; align-items: center;
              margin-bottom: 24px; }
    .header h1 { font-size: 24px; font-weight: 600; }
    .login-box { max-width: 400px; margin: 100px auto; padding: 32px;
                 background: #1e293b; border-radius: 12px; }
    .login-box input { width: 100%; padding: 12px; margin: 8px 0;
                        background: #0f172a; border: 1px solid #334155;
                        border-radius: 8px; color: #e2e8f0; }
    .login-box button { width: 100%; padding: 12px; margin-top: 12px;
                         background: #3b82f6; color: white; border: none;
                         border-radius: 8px; cursor: pointer; font-size: 16px; }
    .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                  gap: 16px; margin-bottom: 24px; }
    .stat-card { background: #1e293b; padding: 20px; border-radius: 12px; }
    .stat-card .label { font-size: 13px; color: #94a3b8; margin-bottom: 8px; }
    .stat-card .value { font-size: 28px; font-weight: 700; }
    .stat-card .value.online { color: #22c55e; }
    .chart-grid { display: grid; grid-template-columns: 2fr 1fr; gap: 16px; }
    .chart-box { background: #1e293b; padding: 20px; border-radius: 12px;
                 margin-bottom: 16px; }
    .chart-box h3 { font-size: 16px; margin-bottom: 16px; color: #cbd5e1; }
    .chart { width: 100%; height: 300px; }
    .full-width { grid-column: 1 / -1; }
    .hidden { display: none !important; }
    @media (max-width: 768px) { .chart-grid { grid-template-columns: 1fr; } }
  </style>
</head>
<body>

  <!-- 登录界面 -->
  <div id="loginView" class="login-box">
    <h2 style="margin-bottom:16px;">Personal Analytics</h2>
    <input type="password" id="tokenInput" placeholder="输入 Admin Token" />
    <button onclick="login()">登录</button>
  </div>

  <!-- 主界面 -->
  <div id="mainView" class="hidden">
    <div class="header">
      <h1>📊 Personal Analytics</h1>
      <button onclick="logout()" style="background:#334155;color:#e2e8f0;border:none;
              padding:8px 16px;border-radius:8px;cursor:pointer;">退出</button>
    </div>

    <!-- 统计卡片 -->
    <div class="stats-grid">
      <div class="stat-card"><div class="label">总访问量 (PV)</div><div class="value" id="totalPv">-</div></div>
      <div class="stat-card"><div class="label">独立访客 (UV)</div><div class="value" id="totalUv">-</div></div>
      <div class="stat-card"><div class="label">今日访问</div><div class="value" id="todayPv">-</div></div>
      <div class="stat-card"><div class="label">今日访客</div><div class="value" id="todayUv">-</div></div>
      <div class="stat-card"><div class="label">当前在线</div><div class="value online" id="onlineCount">-</div></div>
    </div>

    <!-- 趋势图 -->
    <div class="chart-box">
      <h3>最近 30 天访问趋势</h3>
      <div id="trendChart" class="chart"></div>
    </div>

    <div class="chart-grid">
      <!-- 热门页面 -->
      <div class="chart-box">
        <h3>热门页面 TOP 10</h3>
        <div id="pagesChart" class="chart"></div>
      </div>
      <!-- 国家分布 -->
      <div class="chart-box">
        <h3>访客国家 TOP 10</h3>
        <div id="countriesChart" class="chart"></div>
      </div>
      <!-- 设备分布 -->
      <div class="chart-box">
        <h3>设备分布</h3>
        <div id="devicesChart" class="chart"></div>
      </div>
      <!-- 来源网站 -->
      <div class="chart-box">
        <h3>来源网站</h3>
        <div id="referrersChart" class="chart"></div>
      </div>
    </div>
  </div>

<script>
const API_BASE = '';  // 同域，留空即可
let authToken = localStorage.getItem('pa_admin_token') || '';

// ====== 登录 ======
function login() {
  const token = document.getElementById('tokenInput').value.trim();
  if (!token) return;
  authToken = token;
  localStorage.setItem('pa_admin_token', token);
  showMain();
  loadAll();
}

function logout() {
  authToken = '';
  localStorage.removeItem('pa_admin_token');
  document.getElementById('mainView').classList.add('hidden');
  document.getElementById('loginView').classList.remove('hidden');
}

function showMain() {
  document.getElementById('loginView').classList.add('hidden');
  document.getElementById('mainView').classList.remove('hidden');
}

// 已登录则直接进入
if (authToken) { showMain(); loadAll(); }

// ====== API 请求 ======
async function api(path) {
  const res = await fetch(API_BASE + path, {
    headers: { 'Authorization': 'Bearer ' + authToken }
  });
  if (res.status === 401) { logout(); throw new Error('unauthorized'); }
  return res.json();
}

// ====== 加载所有数据 ======
async function loadAll() {
  try {
    const [overview, trend, pages, countries, devices, referrers] = await Promise.all([
      api('/stats'),
      api('/stats/trend?days=30'),
      api('/stats/pages'),
      api('/stats/countries'),
      api('/stats/devices'),
      api('/stats/referrers'),
    ]);

    // 统计卡片
    document.getElementById('totalPv').textContent = formatNum(overview.total_pv);
    document.getElementById('totalUv').textContent = formatNum(overview.total_uv);
    document.getElementById('todayPv').textContent = formatNum(overview.today_pv);
    document.getElementById('todayUv').textContent = formatNum(overview.today_uv);
    document.getElementById('onlineCount').textContent = overview.online;

    // 趋势图
    renderTrend(trend);
    // 热门页面
    renderHorizontalBar('pagesChart', pages.map(p => ({ name: p.page, value: p.pv })), 'PV');
    // 国家
    renderPie('countriesChart', countries.slice(0, 10).map(c => ({ name: c.country, value: c.uv })));
    // 设备
    renderPie('devicesChart', devices.map(d => ({ name: d.device_type, value: d.pv })));
    // 来源
    renderHorizontalBar('referrersChart', referrers.slice(0, 10).map(r => ({
      name: truncate(r.referrer, 30), value: r.pv
    })), 'PV');

  } catch (e) {
    console.error('load failed:', e);
  }
}

// 每 30 秒刷新在线人数
setInterval(async () => {
  if (!authToken) return;
  try {
    const online = await api('/stats/online');
    document.getElementById('onlineCount').textContent = online.online;
  } catch (e) {}
}, 30000);

// ====== 图表渲染 ======
function renderTrend(data) {
  const chart = echarts.init(document.getElementById('trendChart'), 'dark');
  chart.setOption({
    tooltip: { trigger: 'axis' },
    legend: { data: ['PV', 'UV'], textStyle: { color: '#94a3b8' } },
    grid: { left: '3%', right: '4%', bottom: '3%', containLabel: true },
    xAxis: { type: 'category', data: data.map(d => d.day),
             axisLine: { lineStyle: { color: '#334155' } } },
    yAxis: { type: 'value', axisLine: { lineStyle: { color: '#334155' } },
             splitLine: { lineStyle: { color: '#1e293b' } } },
    series: [
      { name: 'PV', type: 'line', smooth: true, data: data.map(d => d.pv),
        itemStyle: { color: '#3b82f6' }, areaStyle: { opacity: 0.1 } },
      { name: 'UV', type: 'line', smooth: true, data: data.map(d => d.uv),
        itemStyle: { color: '#22c55e' }, areaStyle: { opacity: 0.1 } },
    ],
  });
}

function renderPie(elId, data) {
  const chart = echarts.init(document.getElementById(elId), 'dark');
  chart.setOption({
    tooltip: { trigger: 'item' },
    series: [{
      type: 'pie', radius: ['40%', '70%'],
      label: { color: '#94a3b8' },
      data: data,
    }],
  });
}

function renderHorizontalBar(elId, data, label) {
  const chart = echarts.init(document.getElementById(elId), 'dark');
  chart.setOption({
    tooltip: { trigger: 'axis', axisPointer: { type: 'shadow' } },
    grid: { left: '3%', right: '4%', bottom: '3%', containLabel: true },
    xAxis: { type: 'value', axisLine: { lineStyle: { color: '#334155' } },
             splitLine: { lineStyle: { color: '#1e293b' } } },
    yAxis: { type: 'category', data: data.map(d => d.name).reverse(),
             axisLine: { lineStyle: { color: '#334155' } },
             axisLabel: { color: '#94a3b8', fontSize: 11 } },
    series: [{
      name: label, type: 'bar', data: data.map(d => d.value).reverse(),
      itemStyle: { color: '#3b82f6', borderRadius: [0, 4, 4, 0] },
    }],
  });
}

// ====== 工具函数 ======
function formatNum(n) { return n.toLocaleString(); }
function truncate(str, len) { return str.length > len ? str.substring(0, len) + '...' : str; }

// 回车登录
document.getElementById('tokenInput').addEventListener('keypress', e => {
  if (e.key === 'Enter') login();
});
</script>
</body>
</html>
```

**后台功能清单**：

| 模块 | 功能 |
|------|------|
| 概览卡片 | 总 PV、总 UV、今日 PV、今日 UV、当前在线（每 30 秒自动刷新） |
| 趋势图 | 最近 30 天 PV/UV 双折线图 |
| 热门页面 | TOP 10 页面横向柱状图 |
| 国家分布 | TOP 10 国家环形图（按 UV） |
| 设备分布 | Desktop/Mobile/Tablet 环形图 |
| 来源网站 | TOP 10 referrer 横向柱状图 |
| 鉴权 | Token 登录，Token 存 localStorage，401 自动登出 |

---

## 9. 部署步骤（完整命令）

### 9.1 前置准备

```bash
# 1. 安装 Node.js (>= 18) 和 npm
node --version
npm --version

# 2. 全局安装 wrangler（或使用 npx）
npm install -g wrangler

# 3. 登录 Cloudflare
wrangler login
# 浏览器会打开授权页面，点击 Allow
```

### 9.2 创建项目

```bash
# 创建项目目录
mkdir personal-analytics && cd personal-analytics

# 初始化 npm
npm init -y

# 安装依赖（wrangler 作为开发依赖）
npm install --save-dev wrangler
```

### 9.3 创建 D1 数据库

```bash
# 创建数据库
wrangler d1 create personal_analytics

# 输出示例：
# ✅ Successfully created DB 'personal_analytics'
# database_id = xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# 记下这个 database_id，后面配置要用
```

### 9.4 执行建表脚本

```bash
# 本地开发数据库（可选，用于本地测试）
wrangler d1 execute personal_analytics --local --file=./schema.sql

# 远程生产数据库
wrangler d1 execute personal_analytics --remote --file=./schema.sql

# 验证表是否创建成功
wrangler d1 execute personal_analytics --remote --command="SELECT name FROM sqlite_master WHERE type='table';"
```

### 9.5 配置 wrangler.toml

```toml
name = "personal-analytics"
main = "src/index.js"
compatibility_date = "2026-09-09"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "personal_analytics"
database_id = "你的-database_id"

# 自定义域名（Worker 会自动创建 DNS 记录和证书）
routes = [
  { pattern = "stats.example.com", custom_domain = true }
]

# 定时任务：每 5 分钟清理在线访客
[triggers]
crons = ["*/5 * * * *"]
```

> 将 `stats.example.com` 替换为你的实际子域名。确保 `example.com` 的 DNS 已托管在 Cloudflare。

### 9.6 设置 Admin Token（Secret）

```bash
# 生成一个随机 Token
openssl rand -hex 32
# 输出示例：a1b2c3d4e5f6...

# 设置为 Worker Secret
wrangler secret put ADMIN_TOKEN
# 提示输入时，粘贴上面生成的 token

# 验证
wrangler secret list
```

### 9.7 部署

```bash
# 部署到生产环境
wrangler deploy

# 输出示例：
# ✅ Deployed personal-analytics
#   https://personal-analytics.你的子域.workers.dev
#   https://stats.example.com
```

### 9.8 验证部署

```bash
# 1. 测试采集接口（模拟一次访问）
curl -X POST https://stats.example.com/collect \
  -H "Content-Type: application/json" \
  -H "Origin: https://www.example.com" \
  -d '{
    "visitor_id": "test-visitor-001",
    "page": "/",
    "referrer": "https://github.com/",
    "screen_width": 1920,
    "screen_height": 1080,
    "device_type": "desktop",
    "lang": "zh"
  }'

# 预期返回：{"success":true}

# 2. 验证数据写入
wrangler d1 execute personal_analytics --remote \
  --command="SELECT COUNT(*) AS pv FROM page_views;"

# 3. 测试统计接口（需要 Token）
curl https://stats.example.com/stats \
  -H "Authorization: Bearer 你的ADMIN_TOKEN"

# 4. 访问管理后台
# 浏览器打开 https://stats.example.com/admin，输入 Token 登录
```

### 9.9 接入个人主页

在你的个人主页 HTML 的 `</body>` 前添加：

```html
<!-- 方式一：引用 Worker 上的 tracker.js（推荐，自动更新） -->
<script defer src="https://stats.example.com/tracker.js"></script>

<!-- 方式二：内联脚本（减少一次请求，但更新需要改主页） -->
<script>
  // 将 public/tracker.js 的内容粘贴到这里
</script>
```

然后在主页上添加统计展示组件（可选）：

```html
<div class="stats-widget">
  <span>👁️ <span id="pvCount">加载中...</span></span>
  <span>👤 <span id="uvCount">加载中...</span></span>
  <span>🟢 <span id="onlineCount">加载中...</span></span>
</div>

<script>
// 注意：这个接口需要鉴权，不建议在公开页面暴露 Token
// 方案：在 Worker 中增加一个公开的 /stats/public 接口，只返回总 PV/UV/在线，不暴露详细数据
// 这里先调用公开接口（需要你在 Worker 中添加）
fetch('https://stats.example.com/stats/public')
  .then(r => r.json())
  .then(d => {
    document.getElementById('pvCount').textContent = d.total_pv.toLocaleString();
    document.getElementById('uvCount').textContent = d.total_uv.toLocaleString();
    document.getElementById('onlineCount').textContent = d.online;
  });
</script>
```

> **重要**：不要在公开页面的 JS 中硬编码 `ADMIN_TOKEN`！如果想在主页展示统计数字，应该在 Worker 中新增一个**无需鉴权的公开接口** `/stats/public`，只返回总 PV/UV/在线人数这三个非敏感数字。

---

## 10. 安全与隐私合规

### 10.1 数据安全

| 措施 | 说明 |
|------|------|
| Admin Token 存储 | 使用 `wrangler secret put`，存在 Cloudflare 加密的 Secret Store 中，不写入代码 |
| 来源校验 | 采集接口校验 `Origin` 白名单，防止其他网站滥用 |
| 输入校验 | 所有用户输入通过 `prepare().bind()` 参数化，杜绝 SQL 注入 |
| HTTPS 强制 | Cloudflare 自动提供 HTTPS，可在 Cloudflare Dashboard 设置 Always Use HTTPS |
| D1 Time Travel | 内置 30 天时点恢复，误删数据可回滚 |

### 10.2 隐私合规

**需要关注的法规**：GDPR（欧盟）、CCPA（加州）、中国《个人信息保护法》。

**本系统收集的数据**：
- visitor_id（随机生成的 UUID，不包含个人身份信息）
- 访问页面、来源页面
- 国家/城市（来自 Cloudflare IP 地理库，**不存储原始 IP**）
- User-Agent、屏幕分辨率、浏览器语言
- 访问时间

**合规建议**：

1. **不存储 IP 地址**：当前方案不存储原始 IP，只存储 Cloudflare 提供的国家/城市，这是很好的隐私实践。

2. **尊重 Do Not Track**：前端脚本已实现 DNT 检测，用户开启 DNT 时不采集。

3. **隐私政策声明**：在网站隐私政策中说明使用了自建统计系统，收集匿名化的访问数据。

4. **数据删除权**：提供数据删除机制。可以在管理后台增加"删除某 visitor_id 所有数据"的功能：
   ```sql
   DELETE FROM page_views WHERE visitor_id = ?;
   DELETE FROM daily_visitors WHERE visitor_id = ?;
   DELETE FROM online_visitors WHERE visitor_id = ?;
   DELETE FROM events WHERE visitor_id = ?;
   ```

5. **数据保留期**：当前设置 90 天自动清理 `page_views` 明细，可根据合规要求调整。

6. **Cookie 声明**：由于采用了 `localStorage` 方案（非 Cookie），在严格的 Cookie 法规下可能仍需声明。建议在隐私政策中说明使用了本地存储。

### 10.3 管理后台安全升级路线

- **V1**：Bearer Token（当前方案，简单够用）
- **V2**：Cloudflare Access（零信任身份验证，支持 Google/GitHub 登录，免费版支持 50 用户）
- **V3**：Cloudflare Access + 审计日志 + IP 白名单

配置 Cloudflare Access：
```bash
# 在 Cloudflare Dashboard → Zero Trust → Access → Applications
# 添加 Application：Self-hosted
# 域名：stats.example.com
# 路径：admin
# 认证方式：Google / GitHub / One-time PIN
```

---

## 11. 性能优化

### 11.1 已实现的优化

| 优化点 | 实现方式 | 效果 |
|--------|----------|------|
| 异步写入 | `ctx.waitUntil()` | 采集响应立即返回，用户无感知延迟 |
| 聚合查询 | `daily_stats` 表 | 总 PV/UV 只需扫描几十行，而非全表 |
| 索引 | `created_at`/`page`/`country`/`visitor_id` | 查询速度提升 10-100 倍 |
| sendBeacon | 前端优先使用 | 页面关闭也能发送，不阻塞页面 |
| Bot 过滤 | 查询时 `WHERE device_type != 'bot'` | 统计数据更准确 |
| 定时清理 | Cron Trigger 每 5 分钟 | 防止在线表无限增长 |

### 11.2 进一步优化（高流量时）

1. **批量写入**：如果日 PV > 10,000，可考虑用 Cloudflare Queues 缓冲采集数据，批量写入 D1，减少数据库写入次数。

2. **缓存统计结果**：在 Worker 中用内存缓存（`globalThis`）缓存 `/stats` 结果 60 秒，减少 D1 查询。

3. **D1 只读副本**：高查询量时，可创建 D1 只读副本（Read Replica），读写分离。

4. **前端脚本压缩**：将 `tracker.js` 压缩混淆，减少体积（当前约 3KB，gzip 后约 1.5KB）。

5. **CDN 缓存 tracker.js**：设置 `Cache-Control: max-age=86400`，利用 Cloudflare CDN 缓存统计脚本。

---

## 12. 成本估算

### 12.1 Cloudflare Workers 免费额度

| 资源 | 免费额度 | 个人站预估消耗 | 占比 |
|------|----------|----------------|------|
| 请求数 | 100,000 / 天 | 500 / 天（含采集+后台查询） | 0.5% |
| CPU 时间 | 10ms / 请求 | < 5ms / 请求（D1 I/O 不计入 CPU） | 50% |
| 内存 | 128MB | < 50MB | 39% |

### 12.2 Cloudflare D1 免费额度

| 资源 | 免费额度 | 个人站预估消耗 | 占比 |
|------|----------|----------------|------|
| Rows Read | 5,000,000 / 天 | ~2,000 / 天（聚合查询+索引扫描） | 0.04% |
| Rows Written | 100,000 / 天 | ~500 / 天（每次采集 5 次写入） | 0.5% |
| 存储 | 5 GB | < 50MB（90 天明细，约 5 万行） | 1% |

### 12.3 其他成本

| 项目 | 费用 |
|------|------|
| 域名 | 已持有（约 $10/年，与统计系统无关） |
| Cloudflare CDN | 免费 |
| SSL 证书 | 免费（Cloudflare 自动签发） |
| D1 Time Travel | 免费（30 天） |
| ECharts CDN | 免费（jsDelivr） |

### 12.4 结论

**对于日 PV < 1,000 的个人主页，本系统可以 100% 运行在 Cloudflare 免费额度内，月成本为 $0。**

即使日 PV 增长到 10,000，仍然在免费额度内（Workers 10 万请求/天，D1 10 万写入/天）。

超出免费额度后的付费价格（Workers Paid $5/月）：
- Workers：$0.30 / 百万请求
- D1：$1 / 百万 rows read，$1.50 / 百万 rows written，$0.75 / GB-月 存储

---

## 13. 演进路线 V1 → V2 → V3

### V1：基础统计（现在就做，1-2 天）

**已实现功能**：
- ✅ PV / UV 统计
- ✅ 今日 PV / UV
- ✅ 在线人数
- ✅ 30 天访问趋势
- ✅ 热门页面 TOP 10
- ✅ 国家/地区分布
- ✅ 设备类型分布
- ✅ 来源网站统计
- ✅ 管理后台（ECharts 可视化）
- ✅ Token 鉴权
- ✅ 定时数据清理

**交付物**：
- GitHub 仓库 `personal-analytics`
- 可部署的 Worker + D1
- 前端采集脚本 `tracker.js`
- 管理后台页面

### V2：增强分析（1-2 周）

**新增功能**：
- 📊 页面停留时长统计
- 📊 访问路径分析（用户从哪个页面到哪个页面）
- 📊 Session 会话统计（30 分钟无操作视为新会话）
- 📊 城市级分布（当前已有 city 字段，后台增加展示）
- 📊 浏览器/操作系统分布
- 📊 屏幕分辨率分布
- 📊 小时级访问热力图（一天 24 小时分布）
- 📊 数据导出（CSV / JSON）
- 🔐 升级 Cloudflare Access 鉴权
- 💾 历史数据归档到 R2

### V3：事件驱动分析（1 个月+）

**新增功能**：
- 🎯 自定义事件追踪（按钮点击、下载、外链跳转）
- 🎯 转化漏斗分析（主页 → 项目页 → GitHub → 简历下载 → 联系）
- 🎯 简历下载转化率
- 🎯 GitHub 链接点击率
- 🎯 项目页停留时长与跳出率
- 🎯 A/B 测试支持
- 🎯 实时访客地图（全球地图实时打点）
- 🎯 异常流量告警（突增/突降通知）
- 🎯 多站点支持（一个 Worker 统计多个网站）

**V3 核心价值**：从"网站计数器"升级为"个人品牌转化分析系统"。

你的个人主页最终目标不是"有多少点击"，而是：
> 让别人了解你 → 看项目 → 看技术能力 → 联系你

V3 可以量化这个漏斗每一步的转化率，帮你优化主页内容和布局。

---

## 14. 服务器资源规划

### 14.1 当前架构中的角色

```
Cloudflare 生态（网站 + 统计）
├── 个人主页     → Cloudflare Pages（静态托管）
└── 统计系统     → Cloudflare Worker + D1（Serverless）

🇸🇬 新加坡服务器 2C1G（暂不参与网站统计）
├── FastAPI / AI API
├── 个人博客后端（如果需要动态内容）
├── 爬虫 / 定时任务
├── 监控 / 健康检查
└── 其他轻量应用

🇨🇳 国内服务器 4C4G（暂不参与公网统计链路）
├── Docker 容器化环境
├── MySQL / Redis（数据存储与实验）
├── Jupyter Notebook（数据分析）
├── 大模型 / AI 实验
└── 其他重计算项目
```

### 14.2 为什么这样分配

| 资源 | 定位 | 理由 |
|------|------|------|
| Cloudflare Pages | 个人主页 | 静态站点全球 CDN，免费，零运维 |
| Cloudflare Worker + D1 | 统计系统 | 与网站同生态，免费额度够用，全球边缘低延迟 |
| 🇸🇬 新加坡 2C1G | 应用/后端 | 海外网络环境好，适合跑 API 和爬虫；2C1G 足够轻量应用 |
| 🇨🇳 国内 4C4G | 数据/实验 | 国内访问快，配置高，适合跑数据库、大模型、数据分析；不暴露公网统计链路避免备案和安全问题 |

### 14.3 未来可能的整合

当 V3 事件分析需要更复杂的计算时（如漏斗分析、路径分析），可以：
- 将 D1 中的原始数据定期同步到国内服务器的 MySQL
- 在国内服务器上用 Python / Jupyter 做深度分析
- 分析结果回写到 D1 供管理后台展示

这样形成：**Cloudflare 负责采集和实时查询，国内服务器负责深度分析**的混合架构。

---

## 15. 常见问题 FAQ

### Q1：为什么不用 Google Analytics / Umami / Plausible？

| 方案 | 优点 | 缺点 |
|------|------|------|
| Google Analytics | 免费、功能强大 | 国内访问不稳定、隐私合规风险、数据属于 Google、UI 复杂 |
| Umami | 开源、自托管、隐私友好 | 需要自己维护服务器和数据库 |
| Plausible | 轻量、隐私友好 | 付费（$9/月起），自托管需要服务器 |
| **本方案 (Worker+D1)** | **完全免费、数据自主、零运维、与 Cloudflare 生态集成** | 需要自己写代码（本文档已提供完整代码） |

对于个人主页，本方案在成本、隐私、可控性上最优。

### Q2：D1 和 SQLite 有什么区别？

D1 是 Cloudflare 托管的 SQLite，本质上就是 SQLite，但：
- 自动复制到全球边缘节点（读取就近）
- 内置 Time Travel（30 天时点恢复）
- 与 Worker 原生集成
- 按用量计费（免费额度大）
- 支持只读副本（Read Replica）

SQL 语法完全兼容标准 SQLite。

### Q3：如果 Cloudflare 宕机了怎么办？

- 采集脚本有 try-catch，采集失败不影响主站浏览
- 可以在前端实现降级：采集失败时暂存到 `localStorage`，网络恢复后批量重发
- Cloudflare 历史可用性 > 99.99%，全球 300+ 节点，单点故障影响有限

### Q4：怎么测试本地开发？

```bash
# 启动本地开发服务器（带 D1 本地数据库）
wrangler dev --local

# 本地数据库执行建表
wrangler d1 execute personal_analytics --local --file=./schema.sql

# 本地测试采集
curl -X POST http://localhost:8787/collect \
  -H "Content-Type: application/json" \
  -H "Origin: http://localhost:3000" \
  -d '{"visitor_id":"test-001","page":"/"}'
```

### Q5：visitor_id 存在 localStorage，用户换浏览器或设备怎么办？

这是所有前端统计方案的共同限制。同一个用户在不同设备/浏览器上会被计为不同的 UV。

如果需要跨设备识别，可以：
- 要求用户登录（个人主页一般不需要）
- 用指纹识别（FingerprintJS，但隐私争议大，且准确率不是 100%）
- 接受这个限制（个人主页统计，UV 略有偏差可接受）

### Q6：怎么防止别人刷我的统计数据？

当前已有的防护：
- Origin 白名单校验
- Bot User-Agent 过滤

进一步防护（V2）：
- 频率限制：同一 visitor_id 每秒最多 N 次采集
- Cloudflare Rate Limiting Rules：在 Cloudflare Dashboard 配置
- 异常检测：短时间内来自同一国家/IP 的大量请求标记为可疑

### Q7：管理后台的 Token 存 localStorage 安全吗？

对于个人使用的管理后台，Token 存 localStorage 是可接受的。风险点：
- XSS 攻击可能窃取 Token（但管理后台只有你自己用，且代码可控）
- 共享电脑上未退出登录可能被他人使用

更安全的方案是 V2 升级 Cloudflare Access，用 Google/GitHub 账号登录，完全不依赖 Token。

### Q8：可以统计 404 页面和错误吗？

可以。在前端添加全局错误监听：

```javascript
window.addEventListener('error', function(e) {
  pa.track('js_error', e.message + ' @ ' + e.filename + ':' + e.lineno);
});

// 404 统计：在采集时检查页面状态，或者服务端返回 404 时记录
```

### Q9：数据可以迁移到其他数据库吗？

可以。D1 支持导出为 SQLite 文件：

```bash
wrangler d1 export personal_analytics --remote --output=backup.sql
```

然后可以导入到 MySQL / PostgreSQL / 任何支持 SQL 的数据库。

### Q10：这个方案和 Cloudflare Web Analytics 有什么区别？

Cloudflare 自带的 Web Analytics（免费）：
- 不需要写代码，开启即可用
- 但功能有限：只有 PV、UV、国家、页面、来源
- 数据保留 24 小时（免费版）
- 无法自定义事件、无法导出数据、无法自建后台

本方案：
- 需要部署（本文档提供完整代码和步骤）
- 功能完全可定制
- 数据自主可控，可永久保存
- 支持自定义事件、漏斗分析等高级功能
- 管理后台完全自定义

---

## 附录：快速部署 Checklist

- [ ] 1. 安装 Node.js 和 wrangler，登录 Cloudflare
- [ ] 2. 创建 D1 数据库 `personal_analytics`，记下 database_id
- [ ] 3. 执行 `schema.sql` 建表
- [ ] 4. 配置 `wrangler.toml`（填入 database_id 和自定义域名）
- [ ] 5. 生成并设置 `ADMIN_TOKEN` Secret
- [ ] 6. 修改 `src/cors.js` 中的 `ALLOWED_ORIGINS` 为你的网站域名
- [ ] 7. 修改 `public/tracker.js` 中的 `CONFIG.endpoint` 为你的采集域名
- [ ] 8. 运行 `wrangler deploy` 部署
- [ ] 9. 用 curl 测试 `/collect` 接口
- [ ] 10. 用 curl + Token 测试 `/stats` 接口
- [ ] 11. 浏览器打开 `https://stats.example.com/admin` 登录验证
- [ ] 12. 在个人主页引入 `tracker.js`
- [ ] 13. 访问个人主页，确认管理后台能看到实时数据
- [ ] 14. （可选）在主页添加公开统计展示组件

---

> **文档版本**：v1.0
> **最后更新**：2026-09-09
> **适用场景**：个人主页 / 博客 / 作品集网站的自建访问统计系统
#（注：内容由AI生成）
